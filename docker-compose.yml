services:
  vdt-bot:
    build: ./server
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      PORT: 3000
      VIEWER_PATH: /app/viewer.html
    volumes:
      - /tmp/vdt-reports-git:/tmp/vdt-reports-git
